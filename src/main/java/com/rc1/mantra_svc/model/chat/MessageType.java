package com.rc1.mantra_svc.model.chat;

/**
 * All supported message content types.
 */
public enum MessageType {
    TEXT,
    IMAGE,
    FILE,
    EMOJI,
    SYSTEM   // e.g. "User X joined the group"
}
